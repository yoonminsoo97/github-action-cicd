version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/app/
    overwrite: yes

permissions:
  - object: /ubuntu/app/build/libs/project-0.0.1-SNAPSHOT.jar
    pattern: "**"
    owner: ubuntu
    group: ubuntu
  - object: /ubuntu/app/scripts/deploy.sh
    pattern: "**"
    owner: ubuntu
    group: ubuntu

hooks:
  ApplicationStart:
    - location: /ubuntu/app/scripts/deploy.sh
      timeout: 60
      runas: ubuntu