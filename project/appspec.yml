version: 0.0
os: linux
files:
  - source: /
    destination: /ubuntu/app/
    overwrite: yes

permissions:
  - object: /app/build/libs/project-0.0.1-SNAPSHOT.jar
    pattern: "**"
    owner: ubuntu
    group: ubuntu
  - object: /app/scripts/deploy.sh
    pattern: "**"
    owner: ubuntu
    group: ubuntu

hooks:
  ApplicationStart:
    - location: /app/scripts/deploy.sh
      timeout: 60
      runas: ubuntu